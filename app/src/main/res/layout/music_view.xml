<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp"
    android:layout_marginBottom="4dp">

    <!-- Song Thumbnail -->
    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/imageMV"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@drawable/itunes"
        app:shapeAppearanceOverlay="@style/curved"
        android:layout_alignParentStart="true" />

    <!-- Menu Icon (3 dots or add icon) -->
    <ImageView
        android:id="@+id/menu_icon"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@drawable/ic_dots"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        app:menu = "@menu/music_item_menu"

        android:clickable="true"
        android:focusable="true"
        android:padding="4dp" />

    <!-- Song Duration -->
    <TextView
        android:id="@+id/songDuration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/start_tv"
        android:textColor="@color/black"
        android:layout_toStartOf="@id/menu_icon"
        android:layout_alignTop="@id/imageMV"
        android:layout_centerVertical="true"
        android:layout_marginEnd="8dp" />

    <!-- Song Name -->
    <TextView
        android:id="@+id/songNameMV"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/song_name"
        android:textColor="@color/black"
        android:textStyle="bold"
        android:gravity=""
        android:maxLines="1"
        android:ellipsize="end"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="12dp"
        android:layout_marginTop="4dp"
        android:layout_toEndOf="@id/imageMV"
        android:layout_toStartOf="@id/songDuration"
        android:layout_alignTop="@id/imageMV" />

    <!-- Song Album -->
    <TextView
        android:id="@+id/songAlbumMV"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/song_name"
        android:textColor="#808080"
        android:textSize="12sp"
        android:maxLines="1"
        android:layout_marginEnd="12dp"
        android:ellipsize="end"
        android:layout_marginTop="4dp"
        android:layout_below="@id/songNameMV"
        android:layout_alignStart="@id/songNameMV"
        android:layout_alignEnd="@id/songNameMV" />
</RelativeLayout>
